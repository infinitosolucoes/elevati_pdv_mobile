import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:elevatipdvmobile/app/modules/venda/pages/carrinho/carrinho_controller.dart';
import 'package:elevatipdvmobile/app/modules/venda/venda_module.dart';

void main() {
  initModule(VendaModule());
  // CarrinhoController carrinho;
  //
  setUp(() {
    //     carrinho = VendaModule.to.get<CarrinhoController>();
  });

  group('CarrinhoController Test', () {
    //   test("First Test", () {
    //     expect(carrinho, isInstanceOf<CarrinhoController>());
    //   });

    //   test("Set Value", () {
    //     expect(carrinho.value, equals(0));
    //     carrinho.increment();
    //     expect(carrinho.value, equals(1));
    //   });
  });
}
